backstage:
  backstage:
  #   image:
  #     tag: 1.28.4
    
    # extraAppConfig:
    #   - filename: app-config.extra.yaml
    #     configMapRef: extra-app-config

    # extraEnvVarsSecrets:
    #   - backstage-secrets

    extraEnvVars:
      - name: POSTGRES_HOST
        value: backstage-rw
      - name: POSTGRES_PORT
        value: "5432"